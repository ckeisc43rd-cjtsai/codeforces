#include <bits/stdc++.h>
#define pii pair<int, int>
#define int long long
#define ff first
#define ss second
using namespace std;

signed main(){
	ios_base::sync_with_stdio(false);cin.tie(0);
	int t;cin>t;
	while(t--){
		int a;cin>>a;
		vector<int> li(a);
		int cnt=0;
		for(int i=0; i<a; i++){
			int b;cin>>b;
			li[i]=b;
			cnt+=b;
		}
		if(cnt%a){
			cout<<"No\n";
			continue;
		}else{
			vector<int>
		}
	}
}
